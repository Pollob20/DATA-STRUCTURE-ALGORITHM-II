#include <stdio.h>

int example(int n) {

    if(n <= 1)
        return 1;

    return example(n/2) + example(n/2) + n;
}

int main() {

    int n = 8;

    int result = example(n);

    printf("Result = %d", result);

    return 0;
}
